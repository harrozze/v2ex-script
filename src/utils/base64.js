import {Base64} from 'js-base64'

export const base64_encode = Base64.enode
export const base64_decode = Base64.decode

export default {
    base64_encode: base64_encode,
    base64_decode: base64_decode
}
